server:
  port: 4600
spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/aicompany}
    username: ${DB_USER:aicompany}
    password: ${DB_PASSWORD:aicompany}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
jwt:
  secret: ${JWT_SECRET:dev-secret-change-me}